interface:
  display_name: "Repo Bootstrap"
  short_description: "Deprecated compatibility copy. Prefer `.agents/skills/repo-bootstrap` for the current minimal bootstrap workflow."
  default_prompt: "This is a deprecated compatibility copy. Prefer `.agents/skills/repo-bootstrap`. Check for `.codex/repo-bootstrap.yaml`, determine the bootstrap input state, initialize only the minimal missing workflow scaffolding (`AGENTS.md`, `.codex/config.toml`, `docs/code_review.md`, `docs/PLANS.md`, `.codex/repo-bootstrap.yaml`) unless the repo already uses a richer layout, and report created, refreshed, skipped, untouched, partial, or failed outputs."

policy:
  allow_implicit_invocation: false
